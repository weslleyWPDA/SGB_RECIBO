<a href="{{ $link ?? '' }}" class="btn btn-primary botoes" type="{{ $type }}"
    style="background-color:{{ $color }};text-align: center;width: {{ $width ?? '100%' }};
    margin-right: 5px;margin-left: 5px;border:none;font-weight: 800;">{{ $label }}
</a>
